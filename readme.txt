
To start database to terminal(cmd) and type mongod

To start server first you need to input token
for gitbash/linux comand export - guard_jwtPrivateKey=myKey
for windows cmd or powerShell - $env:guard_jwtPrivateKey='myKey'

run server comand - nodemon index.js
